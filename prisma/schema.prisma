// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// define your models

model User {
  id                 String  @id @default(cuid())
  email              String? @unique
  password           String?
  name               String
  pseudo             String @default("Pseudo")
  avatar             String  @default("https://i0.wp.com/sbcf.fr/wp-content/uploads/2018/03/sbcf-default-avatar.png?ssl=1")
  oauth              OAuth?
  twoFactorSecret    String?
  isTwoFactorEnabled Boolean @default(false)
  // friend
  // games []
}

model Game {
  userId
  win
  loss
  totalscore
}

// on peut faire cette logique dans le front ?
model Chat {

}

model Token {
  id    String @id @default(cuid())
  value String @unique
}

model OAuth {
  id          String @id @default(cuid())
  accessToken String
  tokenType   String
  createdAt   Int
  user        User   @relation(fields: [userId], references: [id])
  userId      String @unique
}

/*
  donc je dois faire un model user
  ensuite je dois me poser la question de comment faire le chat (est-ce que c'est totalement dans le front ou est-ce que c'est dans le back dans la db ?)
  goal faire le folder user
  puis faire le model chat si necessaire
*/
